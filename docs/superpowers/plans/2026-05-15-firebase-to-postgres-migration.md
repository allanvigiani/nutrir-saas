# Firebase → PostgreSQL Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace Firestore as the primary database with PostgreSQL (via Prisma), routing all data operations through the existing Express backend, eliminating Firestore security rules entirely while keeping Firebase Auth.

**Architecture:** All Firestore client-side reads/writes move to Express API routes backed by Prisma + PostgreSQL. The frontend replaces `onSnapshot` listeners with `fetch` + `useEffect` refetch-after-mutation pattern. Firebase Admin SDK continues validating ID tokens in the existing `authenticate` middleware. Firebase Auth (login, Google OAuth, password reset) is untouched.

**Tech Stack:** Prisma ORM, PostgreSQL (Neon), Express (existing), Vitest (existing), fetch hooks

**Portabilidade de banco:** Toda comunicação com o banco passa exclusivamente pelo Prisma Client — zero SQL raw, zero features proprietárias de provedor. Para migrar de Neon para Supabase (ou qualquer outro PostgreSQL), basta trocar `DATABASE_URL` no `.env` e rodar `npx prisma migrate deploy`. Nenhuma linha de código muda.

---

## File Map

### New files
- `prisma/schema.prisma` — full database schema (12 models)
- `prisma/migrations/` — auto-generated by Prisma
- `src/server/lib/prisma.ts` — singleton Prisma client
- `src/server/routes/nutritionists.routes.ts` — GET /api/me, PATCH /api/me
- `src/server/routes/patients.routes.ts` — CRUD /api/patients
- `src/server/routes/consultations.routes.ts` — CRUD /api/patients/:id/consultations
- `src/server/routes/meal-plans.routes.ts` — CRUD /api/patients/:id/meal-plans
- `src/server/routes/meal-plan-items.routes.ts` — CRUD /api/meal-plans/:id/items
- `src/server/routes/lab-exams.routes.ts` — CRUD /api/patients/:id/lab-exams
- `src/server/routes/appointments.routes.ts` — CRUD /api/appointments
- `src/server/routes/payments.routes.ts` — CRUD /api/payments
- `src/server/routes/nutrition-calculations.routes.ts` — CRUD /api/patients/:id/calculations
- `src/server/routes/custom-foods.routes.ts` — CRUD /api/custom-foods
- `src/server/routes/settings.routes.ts` — GET/PUT /api/settings
- `src/server/routes/dashboard.routes.ts` — GET /api/dashboard/stats
- `src/server/services/patients.service.ts`
- `src/server/services/consultations.service.ts`
- `src/server/services/meal-plans.service.ts`
- `src/server/services/lab-exams.service.ts`
- `src/server/services/appointments.service.ts`
- `src/server/services/payments.service.ts`
- `src/server/services/custom-foods.service.ts`
- `src/server/services/dashboard.service.ts`
- `src/server/services/nutritionists.service.ts`
- `src/hooks/useApi.ts` — generic fetch hook (loading/error/data + refetch)
- `src/tests/services/patients.service.test.ts`
- `src/tests/services/consultations.service.test.ts`
- `src/tests/services/appointments.service.test.ts`
- `src/tests/services/payments.service.test.ts`
- `src/tests/services/custom-foods.service.test.ts`
- `src/tests/services/dashboard.service.test.ts`

### Modified files
- `package.json` — add @prisma/client, prisma
- `.env` — add DATABASE_URL
- `src/server/register-api-routes.ts` — register all new routes
- `src/contexts/AuthContext.tsx` — replace onSnapshot with GET /api/me
- `src/contexts/SettingsContext.tsx` — replace onSnapshot with GET /api/settings
- `src/pages/Patients.tsx` — replace Firestore with API calls
- `src/pages/PatientProfile.tsx` — replace Firestore with API calls
- `src/pages/Schedule.tsx` — replace Firestore with API calls
- `src/pages/Financial.tsx` — replace Firestore with API calls
- `src/pages/Dashboard.tsx` — replace 6 onSnapshot with GET /api/dashboard/stats
- `src/pages/Settings.tsx` — replace custom_foods Firestore with API
- `src/pages/MealPlanEdit.tsx` — replace Firestore with API
- `src/pages/AdminDashboard.tsx` — replace Firestore with API
- `src/pages/Register.tsx` — already migrated (uses /api/auth/register-profile)
- `src/components/FoodAutocomplete.tsx` — replace onSnapshot with API
- `src/components/CustomFoodDialog.tsx` — replace Firestore with API

---

## FASE 0 — Infraestrutura

### Task 1: Instalar Prisma e PostgreSQL client

**Files:**
- Modify: `package.json`
- Create: `prisma/schema.prisma`
- Create: `src/server/lib/prisma.ts`
- Modify: `.env`

- [ ] **Step 1: Instalar dependências**

```bash
npm install @prisma/client
npm install --save-dev prisma
```

Expected: Packages installed without errors.

- [ ] **Step 2: Inicializar Prisma**

```bash
npx prisma init --datasource-provider postgresql
```

Expected: Creates `prisma/schema.prisma` and adds `DATABASE_URL` to `.env`.

- [ ] **Step 3: Configurar DATABASE_URL no .env**

No `.env`:
```
# Neon (produção/dev)
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx.us-east-2.aws.neon.tech/nutrir_saas?sslmode=require"

# Para migrar de provedor no futuro, só esta linha muda:
# Supabase:  postgresql://postgres:[PASS]@db.[REF].supabase.co:5432/postgres
# Railway:   postgresql://postgres:[PASS]@[HOST]:5432/railway
# Local:     postgresql://postgres:postgres@localhost:5432/nutrir_saas
```

Pegar a connection string em: **Neon Console → Project → Connection Details → Prisma**.

Adicionar `DATABASE_URL` ao `.env.example` sem o valor real:
```
DATABASE_URL="postgresql://..."
```

- [ ] **Step 4: Escrever o schema Prisma completo**

Substituir o conteúdo de `prisma/schema.prisma`:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Nutritionist {
  id                      String    @id
  name                    String
  email                   String    @unique
  crn                     String?
  cpf                     String?
  cnpj                    String?
  phone                   String?
  specialties             String[]
  photoUrl                String?
  role                    String    @default("nutritionist")
  plan                    String    @default("free")
  createdAt               DateTime  @default(now())
  updatedAt               DateTime  @updatedAt
  lastLogin               DateTime?
  subscriptionId          String?
  subscriptionStatus      String?
  cancelAtPeriodEnd       Boolean?
  currentPeriodEnd        DateTime?
  firstSubscriptionDate   DateTime?
  hadRefundBefore         Boolean?
  lastSubscriptionCheck   DateTime?
  googleCalendarTokens    Json?
  googleCalendarConnected Boolean?
  hasSeenTutorial         Boolean?
  welcomeEmailSentAt      DateTime?

  patients     Patient[]
  appointments Appointment[]
  payments     Payment[]
  customFoods  CustomFood[]

  @@map("nutritionists")
}

model Patient {
  id             String   @id @default(cuid())
  nutritionistId String
  nutritionist   Nutritionist @relation(fields: [nutritionistId], references: [id])
  name           String
  birthDate      String
  gender         String
  cpf            String
  phone          String
  email          String
  address        String
  objective      String
  activityLevel  String
  diseases       String?
  medications    String?
  allergies      String?
  photoUrl       String?
  status         String   @default("active")
  accessToken    String?
  uid            String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  consultations  Consultation[]
  mealPlans      MealPlan[]
  labExams       LabExam[]
  appointments   Appointment[]
  calculations   NutritionCalculation[]

  @@map("patients")
}

model Consultation {
  id             String   @id @default(cuid())
  patientId      String
  patient        Patient  @relation(fields: [patientId], references: [id], onDelete: Cascade)
  nutritionistId String
  date           String
  weight         Float
  height         Float
  imc            Float
  fatPercentage  Float?
  waist          Float?
  hip            Float?
  abdomen        Float?
  arm            Float?
  anamnesis      String
  observations   String?
  complaints     String?
  objectives     String?
  status         String
  accessToken    String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  calculations   NutritionCalculation[]

  @@map("consultations")
}

model MealPlan {
  id                  String   @id @default(cuid())
  patientId           String
  patient             Patient  @relation(fields: [patientId], references: [id], onDelete: Cascade)
  nutritionistId      String
  name                String
  generalInstructions String?
  waterIntake         String?
  mealObservations    Json?
  status              String   @default("active")
  accessToken         String?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  items MealPlanItem[]

  @@map("meal_plans")
}

model MealPlanItem {
  id             String   @id @default(cuid())
  mealPlanId     String
  mealPlan       MealPlan @relation(fields: [mealPlanId], references: [id], onDelete: Cascade)
  nutritionistId String
  meal           String
  food           String
  quantity       String
  unit           String
  kcal           Float?
  protein        Float?
  carbs          Float?
  fat            Float?
  baseKcal       Float?
  baseProtein    Float?
  baseCarbs      Float?
  baseFat        Float?
  baseQuantity   Float?
  servingName    String?
  servingWeight  Float?

  @@map("meal_plan_items")
}

model LabExam {
  id             String   @id @default(cuid())
  patientId      String
  patient        Patient  @relation(fields: [patientId], references: [id], onDelete: Cascade)
  nutritionistId String
  date           String
  title          String
  observations   String?
  markers        Json
  reportUrl      String?
  accessToken    String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@map("lab_exams")
}

model Appointment {
  id             String       @id @default(cuid())
  patientId      String
  patient        Patient      @relation(fields: [patientId], references: [id], onDelete: Cascade)
  nutritionistId String
  nutritionist   Nutritionist @relation(fields: [nutritionistId], references: [id])
  date           DateTime
  status         String
  googleEventId  String?
  meetLink       String?
  accessToken    String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  @@map("appointments")
}

model Payment {
  id             String       @id @default(cuid())
  patientId      String
  nutritionistId String
  nutritionist   Nutritionist @relation(fields: [nutritionistId], references: [id])
  amount         Float
  date           DateTime
  method         String
  status         String
  description    String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  @@map("payments")
}

model NutritionCalculation {
  id             String        @id @default(cuid())
  patientId      String
  patient        Patient       @relation(fields: [patientId], references: [id], onDelete: Cascade)
  consultationId String?
  consultation   Consultation? @relation(fields: [consultationId], references: [id])
  nutritionistId String
  name           String
  input          Json
  result         Json
  createdAt      DateTime      @default(now())

  @@map("nutrition_calculations")
}

model CustomFood {
  id             String       @id @default(cuid())
  nutritionistId String
  nutritionist   Nutritionist @relation(fields: [nutritionistId], references: [id])
  name           String
  kcal           Float
  protein        Float
  carbs          Float
  fat            Float
  baseUnit       String
  baseQuantity   Float
  serving        Json?

  @@map("custom_foods")
}

model GlobalSettings {
  id   String @id @default("global")
  data Json

  @@map("settings")
}
```

- [ ] **Step 5: Criar singleton do cliente Prisma**

Criar `src/server/lib/prisma.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };

export const prisma = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma;
}
```

- [ ] **Step 6: Rodar migration inicial**

```bash
npx prisma migrate dev --name init
```

Expected: Migration file created in `prisma/migrations/`, tables created no banco.

- [ ] **Step 7: Gerar client Prisma**

```bash
npx prisma generate
```

Expected: `@prisma/client` gerado sem erros.

- [ ] **Step 8: Verificar TypeScript**

```bash
npm run lint
```

Expected: Sem erros de tipo.

- [ ] **Step 9: Commit**

```bash
git add prisma/ src/server/lib/prisma.ts package.json package-lock.json .env.example
git commit -m "feat: add Prisma + PostgreSQL infrastructure"
```

---

### Task 2: Hook genérico de fetch para o frontend

**Files:**
- Create: `src/hooks/useApi.ts`

- [ ] **Step 1: Criar hook useApi**

Criar `src/hooks/useApi.ts`:

```typescript
import { useState, useEffect, useCallback, useRef } from 'react';
import { auth } from '../lib/firebase';

interface UseApiOptions {
  immediate?: boolean;
}

interface UseApiState<T> {
  data: T | null;
  loading: boolean;
  error: string | null;
}

export function useApi<T>(
  url: string,
  options: UseApiOptions = { immediate: true }
) {
  const [state, setState] = useState<UseApiState<T>>({
    data: null,
    loading: options.immediate ?? true,
    error: null,
  });

  const fetchData = useCallback(async () => {
    setState(prev => ({ ...prev, loading: true, error: null }));
    try {
      const token = await auth.currentUser?.getIdToken();
      const res = await fetch(url, {
        headers: { Authorization: `Bearer ${token}` },
      });
      if (!res.ok) {
        const err = await res.json().catch(() => ({}));
        throw new Error(err.error || `HTTP ${res.status}`);
      }
      const data: T = await res.json();
      setState({ data, loading: false, error: null });
    } catch (err: any) {
      setState(prev => ({ ...prev, loading: false, error: err.message }));
    }
  }, [url]);

  useEffect(() => {
    if (options.immediate !== false) {
      fetchData();
    }
  }, [fetchData, options.immediate]);

  return { ...state, refetch: fetchData };
}

export async function apiRequest<T = void>(
  url: string,
  method: string,
  body?: unknown
): Promise<T> {
  const token = await auth.currentUser?.getIdToken();
  const res = await fetch(url, {
    method,
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${token}`,
    },
    body: body !== undefined ? JSON.stringify(body) : undefined,
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error(err.error || `HTTP ${res.status}`);
  }
  if (res.status === 204) return undefined as T;
  return res.json();
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npm run lint
```

Expected: Sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useApi.ts
git commit -m "feat: add generic useApi hook for REST calls"
```

---

## FASE 1 — Contextos App-Wide

### Task 3: Rota backend GET /api/me e PATCH /api/me

**Files:**
- Create: `src/server/services/nutritionists.service.ts`
- Create: `src/server/routes/nutritionists.routes.ts`
- Modify: `src/server/register-api-routes.ts`
- Create: `src/tests/services/nutritionists.service.test.ts`

- [ ] **Step 1: Escrever o teste do service**

Criar `src/tests/services/nutritionists.service.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { createNutritionistsService } from '../../server/services/nutritionists.service.ts';

function makePrisma(overrides: Record<string, any> = {}) {
  return {
    nutritionist: {
      findUnique: vi.fn().mockResolvedValue(null),
      update: vi.fn().mockResolvedValue(null),
      ...overrides.nutritionist,
    },
  };
}

describe('NutritionistsService.getMe', () => {
  it('retorna o nutricionista pelo uid', async () => {
    const nutritionist = { id: 'uid1', name: 'Ana', email: 'ana@test.com', plan: 'free' };
    const prisma = makePrisma({ nutritionist: { findUnique: vi.fn().mockResolvedValue(nutritionist) } });
    const service = createNutritionistsService({ prisma: prisma as any });
    const result = await service.getMe('uid1');
    expect(result).toEqual(nutritionist);
    expect(prisma.nutritionist.findUnique).toHaveBeenCalledWith({ where: { id: 'uid1' } });
  });

  it('lança erro se nutricionista não existe', async () => {
    const prisma = makePrisma();
    const service = createNutritionistsService({ prisma: prisma as any });
    await expect(service.getMe('unknown')).rejects.toThrow('Nutricionista não encontrado');
  });
});

describe('NutritionistsService.updateMe', () => {
  it('atualiza e retorna nutricionista', async () => {
    const updated = { id: 'uid1', name: 'Ana Silva', email: 'ana@test.com', plan: 'free' };
    const prisma = makePrisma({ nutritionist: { update: vi.fn().mockResolvedValue(updated) } });
    const service = createNutritionistsService({ prisma: prisma as any });
    const result = await service.updateMe('uid1', { name: 'Ana Silva' });
    expect(result).toEqual(updated);
  });
});
```

- [ ] **Step 2: Rodar o teste para verificar que falha**

```bash
npm run test -- src/tests/services/nutritionists.service.test.ts
```

Expected: FAIL — "Cannot find module nutritionists.service.ts"

- [ ] **Step 3: Criar o service**

Criar `src/server/services/nutritionists.service.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

type Deps = { prisma: PrismaClient };

export function createNutritionistsService({ prisma }: Deps) {
  async function getMe(uid: string) {
    const nutritionist = await prisma.nutritionist.findUnique({ where: { id: uid } });
    if (!nutritionist) throw new Error('Nutricionista não encontrado');
    return nutritionist;
  }

  async function updateMe(uid: string, data: Record<string, unknown>) {
    return prisma.nutritionist.update({
      where: { id: uid },
      data: { ...data, updatedAt: new Date() },
    });
  }

  return { getMe, updateMe };
}
```

- [ ] **Step 4: Rodar o teste para verificar que passa**

```bash
npm run test -- src/tests/services/nutritionists.service.test.ts
```

Expected: PASS — 3 tests passing.

- [ ] **Step 5: Criar a rota**

Criar `src/server/routes/nutritionists.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { createNutritionistsService } from '../services/nutritionists.service.ts';
import { prisma } from '../lib/prisma.ts';

export function registerNutritionistsRoutes(deps: BaseRouteDeps) {
  const service = createNutritionistsService({ prisma });

  deps.app.get('/api/me', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.getMe(req.user.uid);
      return res.json(data);
    } catch (err: any) {
      return res.status(404).json({ error: err.message });
    }
  });

  deps.app.patch('/api/me', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.updateMe(req.user.uid, req.body);
      return res.json(data);
    } catch (err: any) {
      return res.status(500).json({ error: err.message });
    }
  });
}
```

- [ ] **Step 6: Registrar a rota**

Em `src/server/register-api-routes.ts`, adicionar:

```typescript
import { registerNutritionistsRoutes } from './routes/nutritionists.routes.ts';
// ... dentro de registerApiRoutes():
registerNutritionistsRoutes(deps);
```

- [ ] **Step 7: Verificar TypeScript**

```bash
npm run lint
```

Expected: Sem erros.

- [ ] **Step 8: Commit**

```bash
git add src/server/services/nutritionists.service.ts src/server/routes/nutritionists.routes.ts src/server/register-api-routes.ts src/tests/services/nutritionists.service.test.ts
git commit -m "feat: add GET /api/me and PATCH /api/me routes"
```

---

### Task 4: Migrar AuthContext do onSnapshot para GET /api/me

**Files:**
- Modify: `src/contexts/AuthContext.tsx`

- [ ] **Step 1: Ler o AuthContext atual**

Abrir `src/contexts/AuthContext.tsx` e identificar:
- O `onSnapshot` na linha ~112 que escuta `nutritionists/${firebaseUser.uid}`
- O estado `nutritionist` que é alimentado pelo listener
- A chamada a `/api/verify-subscription` dentro do callback

- [ ] **Step 2: Substituir onSnapshot por fetch**

Localizar o bloco do `onSnapshot` em `AuthContext.tsx` e substituir por:

```typescript
// Substituir todo o bloco onSnapshot por:
const loadNutritionist = async (firebaseUser: User) => {
  try {
    const token = await firebaseUser.getIdToken();
    const res = await fetch('/api/me', {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (res.ok) {
      const data = await res.json();
      setNutritionist(data);
      // manter chamada de verify-subscription se existir
    }
  } catch (err) {
    console.error('Erro ao carregar nutricionista:', err);
  }
};

// No onAuthStateChanged, chamar:
// loadNutritionist(firebaseUser);
```

> **Nota:** O padrão exato de substituição depende de como o AuthContext está estruturado. O objetivo é: ao detectar login (`onAuthStateChanged`), fazer `fetch('/api/me')` em vez de abrir um `onSnapshot`. Remover o `unsubNutritionist()` do cleanup.

- [ ] **Step 3: Remover imports do Firestore não usados**

Verificar se `onSnapshot`, `doc`, `getDoc` ainda são usados em `AuthContext.tsx`. Remover os que não forem mais necessários.

- [ ] **Step 4: Verificar TypeScript**

```bash
npm run lint
```

Expected: Sem erros.

- [ ] **Step 5: Testar manualmente**

Iniciar o servidor (`npm run dev`) e fazer login. Verificar no Network tab do browser que a request `GET /api/me` é chamada e retorna o nutricionista corretamente.

- [ ] **Step 6: Commit**

```bash
git add src/contexts/AuthContext.tsx
git commit -m "feat: replace AuthContext onSnapshot with GET /api/me"
```

---

### Task 5: Rota GET /api/settings + migrar SettingsContext

**Files:**
- Create: `src/server/routes/settings.routes.ts`
- Modify: `src/server/register-api-routes.ts`
- Modify: `src/contexts/SettingsContext.tsx`

- [ ] **Step 1: Criar rota de settings**

Criar `src/server/routes/settings.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { prisma } from '../lib/prisma.ts';

export function registerSettingsRoutes(deps: BaseRouteDeps) {
  deps.app.get('/api/settings', async (_req: any, res: any) => {
    try {
      const row = await prisma.globalSettings.findUnique({ where: { id: 'global' } });
      return res.json(row?.data ?? {});
    } catch (err: any) {
      return res.status(500).json({ error: err.message });
    }
  });

  deps.app.put('/api/settings', deps.authenticate, async (req: any, res: any) => {
    try {
      const row = await prisma.globalSettings.upsert({
        where: { id: 'global' },
        update: { data: req.body },
        create: { id: 'global', data: req.body },
      });
      return res.json(row.data);
    } catch (err: any) {
      return res.status(500).json({ error: err.message });
    }
  });
}
```

- [ ] **Step 2: Registrar rota**

Em `src/server/register-api-routes.ts`:

```typescript
import { registerSettingsRoutes } from './routes/settings.routes.ts';
// dentro de registerApiRoutes():
registerSettingsRoutes(deps);
```

- [ ] **Step 3: Migrar SettingsContext**

Em `src/contexts/SettingsContext.tsx`, substituir o `onSnapshot` de `settings/global` por:

```typescript
useEffect(() => {
  if (!isAuthReady) return;
  fetch('/api/settings')
    .then(r => r.json())
    .then(data => setSettings(data))
    .catch(console.error);
}, [isAuthReady]);
```

Remover imports do Firestore não usados.

- [ ] **Step 4: Verificar TypeScript**

```bash
npm run lint
```

Expected: Sem erros.

- [ ] **Step 5: Commit**

```bash
git add src/server/routes/settings.routes.ts src/server/register-api-routes.ts src/contexts/SettingsContext.tsx
git commit -m "feat: replace SettingsContext onSnapshot with GET /api/settings"
```

---

## FASE 2 — Páginas Simples

### Task 6: CRUD de Custom Foods

**Files:**
- Create: `src/server/services/custom-foods.service.ts`
- Create: `src/server/routes/custom-foods.routes.ts`
- Create: `src/tests/services/custom-foods.service.test.ts`
- Modify: `src/server/register-api-routes.ts`
- Modify: `src/pages/Settings.tsx`
- Modify: `src/components/CustomFoodDialog.tsx`
- Modify: `src/components/FoodAutocomplete.tsx`

- [ ] **Step 1: Escrever testes do service**

Criar `src/tests/services/custom-foods.service.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { createCustomFoodsService } from '../../server/services/custom-foods.service.ts';

function makePrisma(overrides: Record<string, any> = {}) {
  return {
    customFood: {
      findMany: vi.fn().mockResolvedValue([]),
      create: vi.fn().mockResolvedValue({ id: 'food1' }),
      update: vi.fn().mockResolvedValue({ id: 'food1' }),
      delete: vi.fn().mockResolvedValue({ id: 'food1' }),
      findFirst: vi.fn().mockResolvedValue(null),
      ...overrides.customFood,
    },
  };
}

describe('CustomFoodsService', () => {
  it('list retorna alimentos do nutricionista', async () => {
    const foods = [{ id: 'f1', name: 'Arroz', nutritionistId: 'uid1' }];
    const prisma = makePrisma({ customFood: { findMany: vi.fn().mockResolvedValue(foods) } });
    const service = createCustomFoodsService({ prisma: prisma as any });
    const result = await service.list('uid1');
    expect(result).toEqual(foods);
    expect(prisma.customFood.findMany).toHaveBeenCalledWith({ where: { nutritionistId: 'uid1' } });
  });

  it('create insere alimento com nutritionistId', async () => {
    const created = { id: 'f1', name: 'Feijão', nutritionistId: 'uid1' };
    const prisma = makePrisma({ customFood: { create: vi.fn().mockResolvedValue(created) } });
    const service = createCustomFoodsService({ prisma: prisma as any });
    const result = await service.create('uid1', { name: 'Feijão', kcal: 76, protein: 4.8, carbs: 13.6, fat: 0.5, baseUnit: 'g', baseQuantity: 100 });
    expect(result.nutritionistId).toBe('uid1');
  });

  it('delete lança erro se alimento não pertence ao nutricionista', async () => {
    const prisma = makePrisma(); // findFirst retorna null
    const service = createCustomFoodsService({ prisma: prisma as any });
    await expect(service.remove('uid1', 'food-other')).rejects.toThrow('Não autorizado');
  });
});
```

- [ ] **Step 2: Rodar testes — verificar falha**

```bash
npm run test -- src/tests/services/custom-foods.service.test.ts
```

Expected: FAIL — "Cannot find module"

- [ ] **Step 3: Criar o service**

Criar `src/server/services/custom-foods.service.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

type Deps = { prisma: PrismaClient };

type CreateFoodInput = {
  name: string; kcal: number; protein: number; carbs: number;
  fat: number; baseUnit: string; baseQuantity: number; serving?: unknown;
};

export function createCustomFoodsService({ prisma }: Deps) {
  async function list(nutritionistId: string) {
    return prisma.customFood.findMany({ where: { nutritionistId } });
  }

  async function create(nutritionistId: string, data: CreateFoodInput) {
    return prisma.customFood.create({ data: { ...data, nutritionistId, serving: data.serving ?? undefined } });
  }

  async function update(nutritionistId: string, id: string, data: Partial<CreateFoodInput>) {
    const existing = await prisma.customFood.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.customFood.update({ where: { id }, data });
  }

  async function remove(nutritionistId: string, id: string) {
    const existing = await prisma.customFood.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.customFood.delete({ where: { id } });
  }

  return { list, create, update, remove };
}
```

- [ ] **Step 4: Rodar testes — verificar aprovação**

```bash
npm run test -- src/tests/services/custom-foods.service.test.ts
```

Expected: PASS — 3 tests.

- [ ] **Step 5: Criar rota**

Criar `src/server/routes/custom-foods.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { createCustomFoodsService } from '../services/custom-foods.service.ts';
import { prisma } from '../lib/prisma.ts';

export function registerCustomFoodsRoutes(deps: BaseRouteDeps) {
  const service = createCustomFoodsService({ prisma });

  deps.app.get('/api/custom-foods', deps.authenticate, async (req: any, res: any) => {
    const data = await service.list(req.user.uid);
    return res.json(data);
  });

  deps.app.post('/api/custom-foods', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.create(req.user.uid, req.body);
      return res.status(201).json(data);
    } catch (err: any) {
      return res.status(400).json({ error: err.message });
    }
  });

  deps.app.patch('/api/custom-foods/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.update(req.user.uid, req.params.id, req.body);
      return res.json(data);
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });

  deps.app.delete('/api/custom-foods/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      await service.remove(req.user.uid, req.params.id);
      return res.status(204).send();
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });
}
```

- [ ] **Step 6: Registrar rota e migrar componentes**

Em `src/server/register-api-routes.ts`:
```typescript
import { registerCustomFoodsRoutes } from './routes/custom-foods.routes.ts';
// dentro de registerApiRoutes():
registerCustomFoodsRoutes(deps);
```

Em `src/pages/Settings.tsx` — substituir o `onSnapshot` de custom_foods por:
```typescript
const { data: customFoods = [], refetch } = useApi<CustomFood[]>('/api/custom-foods');
```

Em `src/components/FoodAutocomplete.tsx` — substituir `onSnapshot` por:
```typescript
const { data: customFoods = [] } = useApi<CustomFood[]>('/api/custom-foods');
```

Em `src/components/CustomFoodDialog.tsx` — substituir `addDoc`/`updateDoc`/`deleteDoc` por chamadas `apiRequest`.

- [ ] **Step 7: Verificar TypeScript e commit**

```bash
npm run lint
git add src/server/services/custom-foods.service.ts src/server/routes/custom-foods.routes.ts src/tests/services/custom-foods.service.test.ts src/server/register-api-routes.ts src/pages/Settings.tsx src/components/CustomFoodDialog.tsx src/components/FoodAutocomplete.tsx
git commit -m "feat: migrate custom foods from Firestore to PostgreSQL"
```

---

### Task 7: CRUD de Payments / Financial.tsx

**Files:**
- Create: `src/server/services/payments.service.ts`
- Create: `src/server/routes/payments.routes.ts`
- Create: `src/tests/services/payments.service.test.ts`
- Modify: `src/server/register-api-routes.ts`
- Modify: `src/pages/Financial.tsx`

- [ ] **Step 1: Escrever testes**

Criar `src/tests/services/payments.service.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { createPaymentsService } from '../../server/services/payments.service.ts';

function makePrisma(overrides: Record<string, any> = {}) {
  return {
    payment: {
      findMany: vi.fn().mockResolvedValue([]),
      create: vi.fn().mockResolvedValue({ id: 'pay1' }),
      findFirst: vi.fn().mockResolvedValue(null),
      update: vi.fn().mockResolvedValue({ id: 'pay1' }),
      delete: vi.fn().mockResolvedValue({ id: 'pay1' }),
      ...overrides.payment,
    },
  };
}

describe('PaymentsService', () => {
  it('list filtra por nutritionistId e ordena por data desc', async () => {
    const prisma = makePrisma();
    const service = createPaymentsService({ prisma: prisma as any });
    await service.list('uid1');
    expect(prisma.payment.findMany).toHaveBeenCalledWith({
      where: { nutritionistId: 'uid1' },
      orderBy: { date: 'desc' },
    });
  });

  it('create insere payment com nutritionistId', async () => {
    const created = { id: 'pay1', nutritionistId: 'uid1' };
    const prisma = makePrisma({ payment: { create: vi.fn().mockResolvedValue(created) } });
    const service = createPaymentsService({ prisma: prisma as any });
    const result = await service.create('uid1', {
      patientId: 'pat1', amount: 150, date: new Date(), method: 'pix', status: 'paid',
    });
    expect(result.nutritionistId).toBe('uid1');
  });

  it('remove lança erro se payment não pertence ao nutricionista', async () => {
    const prisma = makePrisma();
    const service = createPaymentsService({ prisma: prisma as any });
    await expect(service.remove('uid1', 'pay-other')).rejects.toThrow('Não autorizado');
  });
});
```

- [ ] **Step 2: Rodar — verificar falha**

```bash
npm run test -- src/tests/services/payments.service.test.ts
```

Expected: FAIL

- [ ] **Step 3: Criar service**

Criar `src/server/services/payments.service.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

type Deps = { prisma: PrismaClient };

type CreatePaymentInput = {
  patientId: string; amount: number; date: Date;
  method: string; status: string; description?: string;
};

export function createPaymentsService({ prisma }: Deps) {
  async function list(nutritionistId: string) {
    return prisma.payment.findMany({
      where: { nutritionistId },
      orderBy: { date: 'desc' },
    });
  }

  async function create(nutritionistId: string, data: CreatePaymentInput) {
    return prisma.payment.create({ data: { ...data, nutritionistId } });
  }

  async function update(nutritionistId: string, id: string, data: Partial<CreatePaymentInput>) {
    const existing = await prisma.payment.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.payment.update({ where: { id }, data });
  }

  async function remove(nutritionistId: string, id: string) {
    const existing = await prisma.payment.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.payment.delete({ where: { id } });
  }

  return { list, create, update, remove };
}
```

- [ ] **Step 4: Rodar — verificar aprovação**

```bash
npm run test -- src/tests/services/payments.service.test.ts
```

Expected: PASS — 3 tests.

- [ ] **Step 5: Criar rota e migrar Financial.tsx**

Criar `src/server/routes/payments.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { createPaymentsService } from '../services/payments.service.ts';
import { prisma } from '../lib/prisma.ts';

export function registerPaymentsRoutes(deps: BaseRouteDeps) {
  const service = createPaymentsService({ prisma });

  deps.app.get('/api/payments', deps.authenticate, async (req: any, res: any) => {
    return res.json(await service.list(req.user.uid));
  });

  deps.app.post('/api/payments', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.create(req.user.uid, { ...req.body, date: new Date(req.body.date) });
      return res.status(201).json(data);
    } catch (err: any) {
      return res.status(400).json({ error: err.message });
    }
  });

  deps.app.patch('/api/payments/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      return res.json(await service.update(req.user.uid, req.params.id, req.body));
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });

  deps.app.delete('/api/payments/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      await service.remove(req.user.uid, req.params.id);
      return res.status(204).send();
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });
}
```

Em `src/pages/Financial.tsx` — substituir o `onSnapshot` de payments por:
```typescript
const { data: payments = [], refetch } = useApi('/api/payments');
```
Substituir `addDoc`/`updateDoc`/`deleteDoc` por `apiRequest`.

- [ ] **Step 6: Registrar e commit**

```bash
git add src/server/services/payments.service.ts src/server/routes/payments.routes.ts src/tests/services/payments.service.test.ts src/server/register-api-routes.ts src/pages/Financial.tsx
git commit -m "feat: migrate payments from Firestore to PostgreSQL"
```

---

## FASE 3 — Páginas Médias

### Task 8: CRUD de Appointments / Schedule.tsx

**Files:**
- Create: `src/server/services/appointments.service.ts`
- Create: `src/server/routes/appointments.routes.ts`
- Create: `src/tests/services/appointments.service.test.ts`
- Modify: `src/server/register-api-routes.ts`
- Modify: `src/pages/Schedule.tsx`

- [ ] **Step 1: Escrever testes**

Criar `src/tests/services/appointments.service.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { createAppointmentsService } from '../../server/services/appointments.service.ts';

function makePrisma(overrides: Record<string, any> = {}) {
  return {
    appointment: {
      findMany: vi.fn().mockResolvedValue([]),
      create: vi.fn().mockResolvedValue({ id: 'apt1' }),
      findFirst: vi.fn().mockResolvedValue(null),
      update: vi.fn().mockResolvedValue({ id: 'apt1' }),
      delete: vi.fn().mockResolvedValue({ id: 'apt1' }),
      ...overrides.appointment,
    },
  };
}

describe('AppointmentsService', () => {
  it('list retorna agendamentos do nutricionista ordenados por data', async () => {
    const prisma = makePrisma();
    const service = createAppointmentsService({ prisma: prisma as any });
    await service.list('uid1');
    expect(prisma.appointment.findMany).toHaveBeenCalledWith({
      where: { nutritionistId: 'uid1' },
      orderBy: { date: 'asc' },
      include: { patient: { select: { name: true } } },
    });
  });

  it('create inclui nutritionistId', async () => {
    const prisma = makePrisma({ appointment: { create: vi.fn().mockResolvedValue({ id: 'apt1', nutritionistId: 'uid1' }) } });
    const service = createAppointmentsService({ prisma: prisma as any });
    const result = await service.create('uid1', { patientId: 'pat1', date: new Date(), status: 'confirmed' });
    expect(result.nutritionistId).toBe('uid1');
  });

  it('remove lança erro se agendamento não pertence ao nutricionista', async () => {
    const prisma = makePrisma();
    const service = createAppointmentsService({ prisma: prisma as any });
    await expect(service.remove('uid1', 'apt-other')).rejects.toThrow('Não autorizado');
  });
});
```

- [ ] **Step 2: Rodar — verificar falha**

```bash
npm run test -- src/tests/services/appointments.service.test.ts
```

Expected: FAIL

- [ ] **Step 3: Criar service**

Criar `src/server/services/appointments.service.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

type Deps = { prisma: PrismaClient };

type CreateAppointmentInput = {
  patientId: string; date: Date; status: string;
  googleEventId?: string; meetLink?: string;
};

export function createAppointmentsService({ prisma }: Deps) {
  async function list(nutritionistId: string) {
    return prisma.appointment.findMany({
      where: { nutritionistId },
      orderBy: { date: 'asc' },
      include: { patient: { select: { name: true } } },
    });
  }

  async function create(nutritionistId: string, data: CreateAppointmentInput) {
    return prisma.appointment.create({ data: { ...data, nutritionistId } });
  }

  async function update(nutritionistId: string, id: string, data: Partial<CreateAppointmentInput>) {
    const existing = await prisma.appointment.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.appointment.update({ where: { id }, data });
  }

  async function remove(nutritionistId: string, id: string) {
    const existing = await prisma.appointment.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.appointment.delete({ where: { id } });
  }

  return { list, create, update, remove };
}
```

- [ ] **Step 4: Rodar — verificar aprovação**

```bash
npm run test -- src/tests/services/appointments.service.test.ts
```

Expected: PASS

- [ ] **Step 5: Criar rota**

Criar `src/server/routes/appointments.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { createAppointmentsService } from '../services/appointments.service.ts';
import { prisma } from '../lib/prisma.ts';

export function registerAppointmentsRoutes(deps: BaseRouteDeps) {
  const service = createAppointmentsService({ prisma });

  deps.app.get('/api/appointments', deps.authenticate, async (req: any, res: any) => {
    return res.json(await service.list(req.user.uid));
  });

  deps.app.post('/api/appointments', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.create(req.user.uid, { ...req.body, date: new Date(req.body.date) });
      return res.status(201).json(data);
    } catch (err: any) {
      return res.status(400).json({ error: err.message });
    }
  });

  deps.app.patch('/api/appointments/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.update(req.user.uid, req.params.id, req.body);
      return res.json(data);
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });

  deps.app.delete('/api/appointments/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      await service.remove(req.user.uid, req.params.id);
      return res.status(204).send();
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });
}
```

Em `src/pages/Schedule.tsx` — substituir os 2 `onSnapshot` por `useApi`:
```typescript
const { data: patients = [], refetch: refetchPatients } = useApi('/api/patients');
const { data: appointments = [], refetch: refetchAppointments } = useApi('/api/appointments');
```
Substituir `addDoc`/`updateDoc`/`deleteDoc` por `apiRequest` e chamar `refetchAppointments()` após cada mutação.

- [ ] **Step 6: Registrar e commit**

```bash
git add src/server/services/appointments.service.ts src/server/routes/appointments.routes.ts src/tests/services/appointments.service.test.ts src/server/register-api-routes.ts src/pages/Schedule.tsx
git commit -m "feat: migrate appointments from Firestore to PostgreSQL"
```

---

### Task 9: CRUD de Patients (lista + operações básicas)

**Files:**
- Create: `src/server/services/patients.service.ts`
- Create: `src/server/routes/patients.routes.ts`
- Create: `src/tests/services/patients.service.test.ts`
- Modify: `src/server/register-api-routes.ts`

- [ ] **Step 1: Escrever testes**

Criar `src/tests/services/patients.service.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { createPatientsService } from '../../server/services/patients.service.ts';

function makePrisma(overrides: Record<string, any> = {}) {
  return {
    patient: {
      findMany: vi.fn().mockResolvedValue([]),
      findFirst: vi.fn().mockResolvedValue(null),
      create: vi.fn().mockResolvedValue({ id: 'pat1' }),
      update: vi.fn().mockResolvedValue({ id: 'pat1' }),
      delete: vi.fn().mockResolvedValue({ id: 'pat1' }),
      ...overrides.patient,
    },
  };
}

describe('PatientsService', () => {
  it('list retorna pacientes do nutricionista ordenados por createdAt desc', async () => {
    const prisma = makePrisma();
    const service = createPatientsService({ prisma: prisma as any });
    await service.list('uid1');
    expect(prisma.patient.findMany).toHaveBeenCalledWith({
      where: { nutritionistId: 'uid1' },
      orderBy: { createdAt: 'desc' },
    });
  });

  it('create associa nutritionistId ao paciente', async () => {
    const created = { id: 'pat1', nutritionistId: 'uid1', name: 'João' };
    const prisma = makePrisma({ patient: { create: vi.fn().mockResolvedValue(created) } });
    const service = createPatientsService({ prisma: prisma as any });
    const result = await service.create('uid1', { name: 'João', email: 'joao@test.com' } as any);
    expect(result.nutritionistId).toBe('uid1');
  });

  it('remove lança erro se paciente não pertence ao nutricionista', async () => {
    const prisma = makePrisma();
    const service = createPatientsService({ prisma: prisma as any });
    await expect(service.remove('uid1', 'pat-other')).rejects.toThrow('Não autorizado');
  });
});
```

- [ ] **Step 2: Rodar — verificar falha**

```bash
npm run test -- src/tests/services/patients.service.test.ts
```

- [ ] **Step 3: Criar service**

Criar `src/server/services/patients.service.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

type Deps = { prisma: PrismaClient };

export function createPatientsService({ prisma }: Deps) {
  async function list(nutritionistId: string) {
    return prisma.patient.findMany({
      where: { nutritionistId },
      orderBy: { createdAt: 'desc' },
    });
  }

  async function getOne(nutritionistId: string, id: string) {
    const patient = await prisma.patient.findFirst({ where: { id, nutritionistId } });
    if (!patient) throw new Error('Paciente não encontrado');
    return patient;
  }

  async function create(nutritionistId: string, data: Record<string, unknown>) {
    return prisma.patient.create({ data: { ...(data as any), nutritionistId } });
  }

  async function update(nutritionistId: string, id: string, data: Record<string, unknown>) {
    const existing = await prisma.patient.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.patient.update({ where: { id }, data: data as any });
  }

  async function remove(nutritionistId: string, id: string) {
    const existing = await prisma.patient.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    // Cascade deletes configurados no schema (consultas, planos, exames, etc.)
    return prisma.patient.delete({ where: { id } });
  }

  return { list, getOne, create, update, remove };
}
```

- [ ] **Step 4: Rodar — verificar aprovação**

```bash
npm run test -- src/tests/services/patients.service.test.ts
```

Expected: PASS

- [ ] **Step 5: Criar rota**

Criar `src/server/routes/patients.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { createPatientsService } from '../services/patients.service.ts';
import { prisma } from '../lib/prisma.ts';

export function registerPatientsRoutes(deps: BaseRouteDeps) {
  const service = createPatientsService({ prisma });

  deps.app.get('/api/patients', deps.authenticate, async (req: any, res: any) => {
    return res.json(await service.list(req.user.uid));
  });

  deps.app.get('/api/patients/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      return res.json(await service.getOne(req.user.uid, req.params.id));
    } catch (err: any) {
      return res.status(404).json({ error: err.message });
    }
  });

  deps.app.post('/api/patients', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.create(req.user.uid, req.body);
      return res.status(201).json(data);
    } catch (err: any) {
      return res.status(400).json({ error: err.message });
    }
  });

  deps.app.patch('/api/patients/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      return res.json(await service.update(req.user.uid, req.params.id, req.body));
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });

  deps.app.delete('/api/patients/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      await service.remove(req.user.uid, req.params.id);
      return res.status(204).send();
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });
}
```

- [ ] **Step 6: Registrar e commit**

```bash
git add src/server/services/patients.service.ts src/server/routes/patients.routes.ts src/tests/services/patients.service.test.ts src/server/register-api-routes.ts
git commit -m "feat: add patients CRUD API backed by PostgreSQL"
```

---

### Task 10: Migrar Patients.tsx

**Files:**
- Modify: `src/pages/Patients.tsx`

> **Nota:** Este arquivo tem 741 linhas e batch ops em 6 coleções. Ao deletar um paciente, o Prisma já faz cascade via FK (`onDelete: Cascade` no schema). Não é necessário deletar sub-coleções manualmente.

- [ ] **Step 1: Substituir onSnapshot de patients**

Em `src/pages/Patients.tsx`, substituir os 2 `onSnapshot` por:
```typescript
const { data: patients = [], loading, refetch } = useApi<Patient[]>('/api/patients');
```

- [ ] **Step 2: Substituir operações de escrita**

Substituir todos os `addDoc`, `updateDoc`, `writeBatch`, `deleteDoc` por `apiRequest`:
- Criar paciente: `await apiRequest('/api/patients', 'POST', data)` + `refetch()`
- Atualizar: `await apiRequest('/api/patients/${id}', 'PATCH', data)` + `refetch()`
- Deletar: `await apiRequest('/api/patients/${id}', 'DELETE')` + `refetch()`

> **Nota:** O `writeBatch` que deletava sub-coleções manualmente é eliminado — o PostgreSQL faz cascade automaticamente.

- [ ] **Step 3: Remover imports do Firestore**

Remover todos os imports de `firebase/firestore` que não são mais usados.

- [ ] **Step 4: Verificar TypeScript e commit**

```bash
npm run lint
git add src/pages/Patients.tsx
git commit -m "feat: migrate Patients.tsx from Firestore to REST API"
```

---

### Task 11: Sub-recursos de pacientes (consultas, planos, exames, cálculos)

**Files:**
- Create: `src/server/services/consultations.service.ts`
- Create: `src/server/routes/consultations.routes.ts`
- Create: `src/server/routes/meal-plans.routes.ts`
- Create: `src/server/routes/meal-plan-items.routes.ts`
- Create: `src/server/routes/lab-exams.routes.ts`
- Create: `src/server/routes/nutrition-calculations.routes.ts`
- Create: `src/tests/services/consultations.service.test.ts`
- Modify: `src/server/register-api-routes.ts`
- Modify: `src/pages/MealPlanEdit.tsx`

- [ ] **Step 1: Escrever testes do ConsultationsService**

Criar `src/tests/services/consultations.service.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { createConsultationsService } from '../../server/services/consultations.service.ts';

function makePrisma(overrides: Record<string, any> = {}) {
  return {
    consultation: {
      findMany: vi.fn().mockResolvedValue([]),
      findFirst: vi.fn().mockResolvedValue(null),
      create: vi.fn().mockResolvedValue({ id: 'c1' }),
      update: vi.fn().mockResolvedValue({ id: 'c1' }),
      delete: vi.fn().mockResolvedValue({ id: 'c1' }),
      ...overrides.consultation,
    },
  };
}

describe('ConsultationsService', () => {
  it('list retorna consultas do paciente ordenadas por data desc', async () => {
    const prisma = makePrisma();
    const service = createConsultationsService({ prisma: prisma as any });
    await service.list('uid1', 'pat1');
    expect(prisma.consultation.findMany).toHaveBeenCalledWith({
      where: { patientId: 'pat1', nutritionistId: 'uid1' },
      orderBy: { date: 'desc' },
    });
  });

  it('remove lança erro se consulta não pertence ao nutricionista', async () => {
    const prisma = makePrisma();
    const service = createConsultationsService({ prisma: prisma as any });
    await expect(service.remove('uid1', 'c-other')).rejects.toThrow('Não autorizado');
  });
});
```

- [ ] **Step 2: Rodar — verificar falha**

```bash
npm run test -- src/tests/services/consultations.service.test.ts
```

- [ ] **Step 3: Criar ConsultationsService**

Criar `src/server/services/consultations.service.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

type Deps = { prisma: PrismaClient };

export function createConsultationsService({ prisma }: Deps) {
  async function list(nutritionistId: string, patientId: string) {
    return prisma.consultation.findMany({
      where: { patientId, nutritionistId },
      orderBy: { date: 'desc' },
    });
  }

  async function create(nutritionistId: string, patientId: string, data: Record<string, unknown>) {
    return prisma.consultation.create({
      data: { ...(data as any), patientId, nutritionistId },
    });
  }

  async function update(nutritionistId: string, id: string, data: Record<string, unknown>) {
    const existing = await prisma.consultation.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.consultation.update({ where: { id }, data: data as any });
  }

  async function remove(nutritionistId: string, id: string) {
    const existing = await prisma.consultation.findFirst({ where: { id, nutritionistId } });
    if (!existing) throw new Error('Não autorizado');
    return prisma.consultation.delete({ where: { id } });
  }

  return { list, create, update, remove };
}
```

- [ ] **Step 4: Rodar — verificar aprovação**

```bash
npm run test -- src/tests/services/consultations.service.test.ts
```

Expected: PASS

- [ ] **Step 5: Criar rotas de sub-recursos**

Criar `src/server/routes/consultations.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { createConsultationsService } from '../services/consultations.service.ts';
import { prisma } from '../lib/prisma.ts';

export function registerConsultationsRoutes(deps: BaseRouteDeps) {
  const service = createConsultationsService({ prisma });

  deps.app.get('/api/patients/:patientId/consultations', deps.authenticate, async (req: any, res: any) => {
    return res.json(await service.list(req.user.uid, req.params.patientId));
  });

  deps.app.post('/api/patients/:patientId/consultations', deps.authenticate, async (req: any, res: any) => {
    try {
      const data = await service.create(req.user.uid, req.params.patientId, req.body);
      return res.status(201).json(data);
    } catch (err: any) {
      return res.status(400).json({ error: err.message });
    }
  });

  deps.app.patch('/api/consultations/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      return res.json(await service.update(req.user.uid, req.params.id, req.body));
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });

  deps.app.delete('/api/consultations/:id', deps.authenticate, async (req: any, res: any) => {
    try {
      await service.remove(req.user.uid, req.params.id);
      return res.status(204).send();
    } catch (err: any) {
      return res.status(403).json({ error: err.message });
    }
  });
}
```

Criar `src/server/routes/meal-plans.routes.ts` com o mesmo padrão — rotas:
- `GET /api/patients/:patientId/meal-plans`
- `POST /api/patients/:patientId/meal-plans`
- `PATCH /api/meal-plans/:id`
- `DELETE /api/meal-plans/:id`

Criar `src/server/routes/meal-plan-items.routes.ts`:
- `GET /api/meal-plans/:mealPlanId/items`
- `POST /api/meal-plans/:mealPlanId/items`
- `DELETE /api/meal-plan-items/:id`
- `PATCH /api/meal-plan-items/:id`

Criar `src/server/routes/lab-exams.routes.ts`:
- `GET /api/patients/:patientId/lab-exams`
- `POST /api/patients/:patientId/lab-exams`
- `PATCH /api/lab-exams/:id`
- `DELETE /api/lab-exams/:id`

Criar `src/server/routes/nutrition-calculations.routes.ts`:
- `GET /api/patients/:patientId/calculations`
- `POST /api/patients/:patientId/calculations`
- `DELETE /api/calculations/:id`

- [ ] **Step 6: Registrar todas as rotas e commit**

Em `src/server/register-api-routes.ts`, importar e chamar todas as novas rotas.

```bash
npm run lint
git add src/server/services/consultations.service.ts src/server/routes/ src/tests/services/consultations.service.test.ts src/server/register-api-routes.ts
git commit -m "feat: add sub-resource routes for consultations, meal plans, lab exams, calculations"
```

---

## FASE 4 — Dashboard

### Task 12: Endpoint agregado GET /api/dashboard/stats

**Files:**
- Create: `src/server/services/dashboard.service.ts`
- Create: `src/server/routes/dashboard.routes.ts`
- Create: `src/tests/services/dashboard.service.test.ts`
- Modify: `src/server/register-api-routes.ts`
- Modify: `src/pages/Dashboard.tsx`

- [ ] **Step 1: Escrever testes**

Criar `src/tests/services/dashboard.service.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { createDashboardService } from '../../server/services/dashboard.service.ts';

function makePrisma() {
  return {
    patient: { count: vi.fn().mockResolvedValue(5), findMany: vi.fn().mockResolvedValue([]) },
    appointment: { findMany: vi.fn().mockResolvedValue([]) },
    consultation: { findMany: vi.fn().mockResolvedValue([]) },
    payment: { findMany: vi.fn().mockResolvedValue([]) },
    mealPlan: { count: vi.fn().mockResolvedValue(3) },
  };
}

describe('DashboardService.getStats', () => {
  it('retorna contagem de pacientes ativos', async () => {
    const prisma = makePrisma();
    const service = createDashboardService({ prisma: prisma as any });
    const stats = await service.getStats('uid1');
    expect(prisma.patient.count).toHaveBeenCalledWith({ where: { nutritionistId: 'uid1', status: 'active' } });
    expect(stats.activePatients).toBe(5);
  });

  it('retorna total de planos alimentares ativos', async () => {
    const prisma = makePrisma();
    const service = createDashboardService({ prisma: prisma as any });
    const stats = await service.getStats('uid1');
    expect(stats.activeMealPlans).toBe(3);
  });
});
```

- [ ] **Step 2: Rodar — verificar falha**

```bash
npm run test -- src/tests/services/dashboard.service.test.ts
```

- [ ] **Step 3: Criar DashboardService**

Criar `src/server/services/dashboard.service.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

type Deps = { prisma: PrismaClient };

export function createDashboardService({ prisma }: Deps) {
  async function getStats(nutritionistId: string) {
    const now = new Date();
    const startOfMonth = new Date(now.getFullYear(), now.getMonth(), 1);
    const endOfDay = new Date(now);
    endOfDay.setHours(23, 59, 59, 999);
    const startOfDay = new Date(now);
    startOfDay.setHours(0, 0, 0, 0);

    const [
      activePatients,
      recentPatients,
      todayAppointments,
      monthConsultations,
      monthPayments,
      activeMealPlans,
    ] = await Promise.all([
      prisma.patient.count({ where: { nutritionistId, status: 'active' } }),
      prisma.patient.findMany({
        where: { nutritionistId },
        orderBy: { createdAt: 'desc' },
        take: 5,
        select: { id: true, name: true, createdAt: true, status: true },
      }),
      prisma.appointment.findMany({
        where: { nutritionistId, date: { gte: startOfDay, lte: endOfDay } },
        include: { patient: { select: { name: true } } },
        orderBy: { date: 'asc' },
      }),
      prisma.consultation.findMany({
        where: { nutritionistId, createdAt: { gte: startOfMonth } },
        select: { id: true, createdAt: true },
      }),
      prisma.payment.findMany({
        where: { nutritionistId, status: 'paid', date: { gte: startOfMonth } },
        select: { amount: true },
      }),
      prisma.mealPlan.count({ where: { nutritionistId, status: 'active' } }),
    ]);

    const monthRevenue = monthPayments.reduce((sum, p) => sum + p.amount, 0);

    return {
      activePatients,
      recentPatients,
      todayAppointments,
      monthConsultations: monthConsultations.length,
      consultationDates: monthConsultations.map(c => c.createdAt),
      monthRevenue,
      activeMealPlans,
    };
  }

  return { getStats };
}
```

- [ ] **Step 4: Rodar — verificar aprovação**

```bash
npm run test -- src/tests/services/dashboard.service.test.ts
```

Expected: PASS

- [ ] **Step 5: Criar rota**

Criar `src/server/routes/dashboard.routes.ts`:

```typescript
import type { BaseRouteDeps } from '../types.ts';
import { createDashboardService } from '../services/dashboard.service.ts';
import { prisma } from '../lib/prisma.ts';

export function registerDashboardRoutes(deps: BaseRouteDeps) {
  const service = createDashboardService({ prisma });

  deps.app.get('/api/dashboard/stats', deps.authenticate, async (req: any, res: any) => {
    try {
      return res.json(await service.getStats(req.user.uid));
    } catch (err: any) {
      return res.status(500).json({ error: err.message });
    }
  });
}
```

- [ ] **Step 6: Migrar Dashboard.tsx**

Em `src/pages/Dashboard.tsx`, substituir os 6 `onSnapshot` por um único fetch:

```typescript
const { data: stats, loading, refetch } = useApi('/api/dashboard/stats');

// Extrair do stats:
const activePatients = stats?.activePatients ?? 0;
const recentPatients = stats?.recentPatients ?? [];
const todayAppointments = stats?.todayAppointments ?? [];
const monthConsultations = stats?.monthConsultations ?? 0;
const monthRevenue = stats?.monthRevenue ?? 0;
const activeMealPlans = stats?.activeMealPlans ?? 0;
```

Remover todos os `onSnapshot`, `unsubscribe*`, e imports do Firestore.

- [ ] **Step 7: Registrar e commit**

```bash
git add src/server/services/dashboard.service.ts src/server/routes/dashboard.routes.ts src/tests/services/dashboard.service.test.ts src/server/register-api-routes.ts src/pages/Dashboard.tsx
git commit -m "feat: migrate Dashboard from 6 Firestore listeners to single GET /api/dashboard/stats"
```

---

## FASE 5 — PatientProfile.tsx

### Task 13: Migrar PatientProfile.tsx

**Files:**
- Modify: `src/pages/PatientProfile.tsx` (~2.900 linhas)

> **Nota:** Este é o arquivo mais crítico do projeto. Recomenda-se migrar por seção: (1) dados do paciente, (2) consultas, (3) planos alimentares + itens, (4) exames, (5) cálculos. Não tentar em uma única edição.

- [ ] **Step 1: Substituir onSnapshot do paciente**

O `onSnapshot` na linha ~1036 escuta `patients/${id}`. Substituir por:

```typescript
const { data: patient, loading, refetch: refetchPatient } = useApi<Patient>(`/api/patients/${id}`);
```

- [ ] **Step 2: Substituir getDocs de consultas**

Dentro do antigo callback do `onSnapshot`, há `getDocs` das consultas. Substituir por:

```typescript
const { data: consultations = [], refetch: refetchConsultations } = useApi<Consultation[]>(
  `/api/patients/${id}/consultations`
);
```

- [ ] **Step 3: Substituir getDocs de planos alimentares**

```typescript
const { data: mealPlans = [], refetch: refetchMealPlans } = useApi<MealPlan[]>(
  `/api/patients/${id}/meal-plans`
);
```

- [ ] **Step 4: Substituir getDocs de exames**

```typescript
const { data: exams = [], refetch: refetchExams } = useApi<LabExam[]>(
  `/api/patients/${id}/lab-exams`
);
```

- [ ] **Step 5: Substituir getDocs de cálculos**

```typescript
const { data: calculations = [], refetch: refetchCalculations } = useApi<NutritionCalculation[]>(
  `/api/patients/${id}/calculations`
);
```

- [ ] **Step 6: Substituir todas as escritas**

Para cada operação de escrita no PatientProfile (addDoc, updateDoc, deleteDoc):
- Adicionar consulta: `await apiRequest('/api/patients/${id}/consultations', 'POST', data)` + `refetchConsultations()`
- Atualizar consulta: `await apiRequest('/api/consultations/${consultationId}', 'PATCH', data)` + `refetchConsultations()`
- Deletar consulta: `await apiRequest('/api/consultations/${consultationId}', 'DELETE')` + `refetchConsultations()`
- (mesmo padrão para planos, itens, exames, cálculos)

- [ ] **Step 7: Remover writeBatch**

O `writeBatch` que deleta planos + itens em cascata não é mais necessário — o PostgreSQL faz cascade via FK. Substituir por um único `DELETE /api/meal-plans/:id`.

- [ ] **Step 8: Remover imports do Firestore**

Remover todos os imports `firebase/firestore` não utilizados.

- [ ] **Step 9: Verificar TypeScript**

```bash
npm run lint
```

Expected: Sem erros de tipo.

- [ ] **Step 10: Commit**

```bash
git add src/pages/PatientProfile.tsx
git commit -m "feat: migrate PatientProfile.tsx from Firestore to REST API"
```

---

## FASE 6 — Limpeza Final

### Task 14: Remover dependências do Firestore

**Files:**
- Modify: `src/lib/firebase.ts`
- Modify: `package.json`
- Delete: `firestore.rules`

- [ ] **Step 1: Verificar uso restante do Firestore**

```bash
grep -r "firebase/firestore\|from 'firebase'" src/ --include="*.tsx" --include="*.ts" | grep -v "firebase/auth\|firebase/storage\|firebase/app\|firebase-admin"
```

Expected: Nenhum resultado (ou apenas firebase/auth e firebase/storage).

- [ ] **Step 2: Remover imports do Firestore de firebase.ts**

Em `src/lib/firebase.ts`, remover inicialização do Firestore (`getFirestore`, `db`) se não houver mais uso.

- [ ] **Step 3: Verificar TypeScript final**

```bash
npm run lint
```

Expected: Sem erros.

- [ ] **Step 4: Rodar todos os testes**

```bash
npm run test
```

Expected: Todos os testes passam.

- [ ] **Step 5: Commit final**

```bash
git add -A
git commit -m "chore: remove Firestore client dependencies — migration to PostgreSQL complete"
```

---

## Verificação End-to-End

Após cada fase, verificar manualmente:

| Fluxo | Como testar |
|---|---|
| Login | Entrar com email/senha → perfil carrega sem erro |
| Cadastro | Criar nova conta → documento criado no PostgreSQL |
| Listar pacientes | Abrir `/patients` → lista carrega |
| Criar paciente | Formulário de novo paciente → salvo no banco |
| Deletar paciente | Deletar → consultas, planos, exames removidos em cascade |
| Consultas | Abrir PatientProfile → consultas carregam |
| Dashboard | Abrir `/dashboard` → stats corretos |
| Agenda | Abrir `/schedule` → agendamentos do dia |
| Financeiro | Abrir `/financial` → pagamentos listados |
| Alimentos customizados | Settings → criar/editar/deletar alimento |
