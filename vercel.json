{
  "buildCommand": "npx prisma generate && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!_next|favicon|assets).*)", "destination": "/index.html" }
  ]
}
